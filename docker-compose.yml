version: '3.8'

services:
  app:
    build: .
    container_name: psecret-app
    restart: unless-stopped
    ports:
      - "3006:3000"
    environment:
      - SQLITE_DB_PATH=/app/data/psecret.sqlite
    env_file:
      - .env
    volumes:
      - /home/anla/backup/p.secret:/app/data
